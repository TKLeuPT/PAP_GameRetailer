@model IEnumerable<GameRetailer.Models.Stock>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Jogo.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Jogo.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NumJogo }) |
            @Html.ActionLink("Details", "Details", new { id=item.NumJogo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NumJogo })
        </td>
    </tr>
}

</table>
